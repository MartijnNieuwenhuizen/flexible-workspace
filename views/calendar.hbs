<main>
	<section class="calendar">
		<ul class="calendar-months">
			{{#each months}}
				{{#ifCond this ../currentMonth}}
				    <li><a href="/2016/{{this}}" class="active">{{this}}</a></li>
				{{else}}
				    <li><a href="/2016/{{this}}">{{this}}</a></li>
				{{/ifCond}}
			{{/each}}
		</ul>
		
		<form action="/calendar" method="post" class="calendar-days">
			<ul class="days-title">
				<li>Mon</li>
				<li>Tue</li>
				<li>Wed</li>
				<li>Thu</li>
				<li>Fri</li>
				<li>Sat</li>
				<li>Sun</li>
			</ul>

			<ul class="calendar-container">
				{{#each days}}
					<li class="calendar-days--item {{#if this.class}}{{class}}{{/if}}">
						<input id="{{@key}}" type="checkbox" name="{{this.fullDate}}" {{#if this.present}} checked {{/if}} {{#if this.class}} disabled {{/if}}>
						<label for="{{@key}}">{{@key}}</label>
						<span class="amount" style="width:{{this.indication}}%;">{{this.indication}}%</span>
						<span class="amount-total">{{this.indication}}%</span>
						<!-- <ul>
							{{#each this.avalible}}
								<li>{{this}}</li>
							{{/each}}			
						</ul> -->
					</li>
				{{/each}}
			</ul>
			
			<input type="submit">
		</form>
	</section>
</main>